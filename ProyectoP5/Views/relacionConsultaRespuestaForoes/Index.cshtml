@model IEnumerable<ProyectoP5.Models.relacionConsultaRespuestaForo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.foroConsulata.nombreRemitente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.foroRespuestas.nombreReceptor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.foroConsulata.nombreRemitente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.foroRespuestas.nombreReceptor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.relacionConsultaRespuestaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.relacionConsultaRespuestaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.relacionConsultaRespuestaID })
        </td>
    </tr>
}

</table>
